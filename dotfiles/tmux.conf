set -g prefix C-a
unbind C-b
bind C-a send-prefix

setw -g mode-keys vi
setw -g mode-mouse on
setw -g monitor-activity on

unbind %
bind | split-window -h
bind - split-window -v

bind r source-file ~/.tmux.conf
